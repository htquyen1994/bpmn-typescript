// @ts-ignore – Vite ?inline suffix resolves at build time
import themeCss from './theme.css?inline';

/** Complete set of CSS custom-property tokens for the CSP BPMN Studio. */
export const THEME_CSS: string = themeCss as string;
